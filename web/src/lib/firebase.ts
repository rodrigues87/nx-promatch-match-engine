/**
 * Firebase — desabilitado temporariamente (modo mock).
 * 
 * Para ativar Firebase real:
 * 1. Altere MOCK_MODE para false
 * 2. Descomente a inicialização abaixo
 * 3. Preencha .env.local com as credenciais
 */

export const MOCK_MODE = true
